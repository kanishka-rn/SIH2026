# Copy to frontend/.env.local or frontend/.env. Never commit real values.

VITE_API_BASE_URL=http://localhost:8000

# Browser-safe Supabase client config only.
# Use the publishable key (sb_publishable_...). Never put a secret or service-role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
